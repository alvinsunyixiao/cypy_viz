package comm;

struct model_t {
  string name;
  string path;
}

struct vec3d_t {
  double x;
  double y;
  double z;
}

struct quaternion_t {
  double x;
  double y;
  double z;
  double w;
}

struct pose3d_t {
  vec3d_t       position;
  quaternion_t  quaternion;
}
